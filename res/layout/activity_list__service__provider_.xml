<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/profile_front_design"
    tools:context=".List_Service_Provider_Activity">

    <android.support.v7.widget.Toolbar
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/toolbarListID"
        android:background="#a240c9"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Hire"
            android:textColor="#ffffff"
            android:textSize="20dp"
            android:layout_marginRight="5dp"/>
        <Spinner
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/spinnerListID"
            android:spinnerMode="dialog"
            android:prompt="@string/app_name"
            android:paddingLeft="35dp"
            android:pointerIcon="context_menu"
            android:layout_marginTop="4dp">

        </Spinner>
    </android.support.v7.widget.Toolbar>
  <FrameLayout
      android:layout_width="match_parent"
      android:layout_height="220dp"
      android:id="@+id/mapID"
      android:layout_margin="2dp">

  </FrameLayout>

        <ListView
            android:layout_margin="2dp"
            android:background="#f6f7fc"
            android:id="@+id/list_item_id"
            android:layout_width="match_parent"
            android:layout_height="280dp"></ListView>
</LinearLayout>